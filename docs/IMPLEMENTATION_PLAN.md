# 구현 계획

## 관련 이슈

- #60

## 목표

- Colab에서 `vision` 모듈을 import해서 YOLO 학습과 평가를 실행할 수 있게 한다.
- 노트북에는 실행 순서만 두고, 실제 로직은 `vision/` 모듈에 둔다.
- 마지막에 Colab 사용 가이드라인을 별도 문서로 정리한다.

## Sub-issues

- #61 패키지 뼈대와 공용 설정 진입점
- #62 데이터 준비·분할·리사이즈 파이프라인
- #63 YOLOv8n-seg 학습·평가와 Drive 저장 규칙
- #64 Colab 실행 가이드라인 문서

## 작업 원칙

- 코드의 역할이 보이도록 함수와 파일 경계를 분리한다.
- 나중에 형태를 바꾸기 쉬우도록 설정값은 상수로 모은다.
- 각 단계마다 왜 필요한지 짧은 주석을 남긴다.
- 구현이 진행되면 이 문서의 체크박스를 즉시 갱신한다.

## 구현 순서

- [x] #61 `vision` 패키지 진입점과 모듈 구조를 정리한다.
- [ ] #62 Colab에서 import할 경로와 실행 순서를 문서화한다.
- [x] #62 데이터셋 경로, split, resize, label 변환 규칙을 코드로 정리한다.
- [ ] #63 YOLOv8n-seg 학습/평가 실행 함수를 만든다.
- [ ] #63 Google Drive 저장 위치와 `best.pt` 산출 규칙을 정리한다.
- [ ] #63 FastAPI 서빙 연결을 염두에 둔 weight 사용 방식을 정리한다.
- [ ] #64 Colab 사용 가이드라인 문서를 작성한다.
- [ ] #63/#64 로컬 또는 Colab에서 한 번은 실제 실행 흐름을 확인한다.

## 코드 작성 메모

- 공개 함수에는 짧은 docstring을 붙인다.
- 경로 문자열은 하드코딩하지 말고 상수나 설정으로 분리한다.
- 전처리와 학습은 분리해서, 일부만 다시 실행할 수 있게 만든다.
- 실패 지점은 예외 메시지가 바로 원인 파악에 도움이 되도록 작성한다.
- 노트북은 얇게 유지하고, 재사용 로직은 모듈로 뺀다.

## 완료 기준

- [ ] Colab에서 `vision` 모듈을 import할 수 있다.
- [ ] 데이터셋 입력부터 weight 저장까지 흐름이 하나로 이어진다.
- [ ] `best.pt` 저장 위치와 서빙 연결 방식이 문서화되어 있다.
- [ ] Colab 사용 가이드라인 문서가 존재한다.
- [ ] 불필요한 파일 변경이 없다.
