# Model
MODEL_PATH=vision/runs/detect/
CONFIDENCE_THRESHOLD=0.5
DEVICE=cpu  # cpu / cuda / mps

# YOLOv8
YOLO_MODEL=yolov8n.pt
IMAGE_SIZE=640

# ChromaDB (RAG 연결용)
CHROMA_HOST=localhost
CHROMA_PORT=8001

# 학습 설정
BATCH_SIZE=16
EPOCHS=50