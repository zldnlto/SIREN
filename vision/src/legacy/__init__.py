"""Legacy single-flow helpers kept out of the new Phase 1/2 pipeline.

The new data/ontology modules must not import from this namespace.
These files remain only so older notebooks and scripts can be referenced
without mixing them into the task-separated pipeline.
"""

